import { StrictMode } from "react"
import Card from "./Card"
function Dashboard({fullname,occ,nameChange}){
    return(
    <>
        <Card fullname={name} occ={occ} nameChange={nameChange}/>

    </>
    )
}

export default Dashboard